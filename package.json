{
  "name": "apply-line-automation",
  "version": "1.0.0",
  "description": "Apply Line ka background automation server — daily job discover + draft + WhatsApp summary (Send abhi bhi manual)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
